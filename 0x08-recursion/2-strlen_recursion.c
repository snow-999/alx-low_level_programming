#include "main.h"
/**
 * _strlen_recursion - recusion session
 * @s: task string
 * Return: i
 *
*/
int _strlen_recursion(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	continue;
	}
	return (i);
}
